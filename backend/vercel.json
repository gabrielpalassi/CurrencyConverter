{
  "version": 2,
  "builds": [
    {
      "src": "dist",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "bun run build",
        "outputDirectory": "dist"
      }
    }
  ]
}
