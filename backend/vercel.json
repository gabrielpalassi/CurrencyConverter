{
  "version": 2,
  "builds": [
    {
      "src": "src/index.ts",
      "use": "vercel-bun",
      "config": {
        "buildCommand": "bun run build",
        "outputDirectory": "dist"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/dist/$1" }]
}
