{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "bun run build",
        "outputDirectory": "dist"
      }
    }
  ]
}
