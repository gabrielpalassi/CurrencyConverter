{
  "name": "currencyconverter-backend",
  "type": "module",
  "scripts": {
    "start": "bun --watch src/index.ts",
    "start:prod": "bun dist/index.js || (echo 'The project needs to be built before running. Run `bun run build` to compile.' && exit 1)",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "@gaignoux/currency": "^1.1.0",
    "ecb-euro-exchange-rates": "^4.0.0",
    "hono": "^4.6.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@shared/types": "*"
  }
}
